"""
Django settings for experiment_tracker project.

Generated by 'django-admin startproject' using Django 4.2.29.

For more information on this file, see
https://docs.djangoproject.com/en/4.2/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/4.2/ref/settings/
"""

import os  # Provides functions to interact with the operating system

from pathlib import Path  # Offers an object-oriented way to handle filesystem paths

from dotenv import load_dotenv  # Function to load environment variables from a .env file

load_dotenv()  # Loads variables from a .env file into the environment

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/4.2/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = os.getenv("DJANGO_SECRET_KEY")

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

# List of host/domain names that this Django site can serve
# Keep empty in development, but in production you should add your domain(s)
ALLOWED_HOSTS = []

# Application definition

# List of installed Django applications
INSTALLED_APPS = [
    'django.contrib.admin',         # Enables the Django admin dashboard
    'django.contrib.auth',          # Handles user authentication and permissions
    'django.contrib.contenttypes',  # Tracks all installed models (used by auth & admin)
    'django.contrib.sessions',      # Manages user sessions across requests
    'django.contrib.messages',      # Supports temporary messages (e.g., success alerts)
    'django.contrib.staticfiles',   # Manages static files like CSS, JS, images
    'expt_webapp',     # Your custom Django app (add your app's module name here)
    'rest_framework',   # Django REST Framework for building APIs
    'corsheaders',     # Handles Cross-Origin Resource Sharing (CORS) for API access
]


# Middleware are layers that process requests and responses globally
MIDDLEWARE = [
    'corsheaders.middleware.CorsMiddleware',              # Handles CORS headers for API requests
    'django.middleware.security.SecurityMiddleware',        # Adds basic security enhancements
    'django.contrib.sessions.middleware.SessionMiddleware',# Enables session support
    'django.middleware.common.CommonMiddleware',           # Handles common tasks (URL normalization, etc.)
    'django.middleware.csrf.CsrfViewMiddleware',           # Protects against CSRF attacks
    'django.contrib.auth.middleware.AuthenticationMiddleware', # Associates users with requests
    'django.contrib.messages.middleware.MessageMiddleware',    # Enables message framework
    'django.middleware.clickjacking.XFrameOptionsMiddleware',  # Prevents clickjacking attacks
]

# Points Django to the root URL configuration module for the project
ROOT_URLCONF = 'experiment_tracker.urls'


# Template configuration (controls how Django loads and renders HTML templates)
TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',  # Template engine backend
        'DIRS': [BASE_DIR / 'expt_webapp' / 'templates'],  # Directories where Django will look for global templates
        'APP_DIRS': True,  # Allows Django to load templates from each app's "templates" folder
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',     # Adds debug info to templates
                'django.template.context_processors.request',   # Adds request object to templates
                'django.contrib.auth.context_processors.auth',  # Adds user/auth info
                'django.contrib.messages.context_processors.messages',  # Adds message framework support
            ],
        },
    },
]


# Entry point for WSGI-compatible web servers (used for deployment)
WSGI_APPLICATION = 'experiment_tracker.wsgi.application'


# Database
# https://docs.djangoproject.com/en/4.2/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',  # Use PostgreSQL as the database engine
        'NAME': os.getenv('DB_NAME'),                # Database name from environment variable
        'USER': os.getenv('DB_USER'),                # Database user from environment variable
        'PASSWORD': os.getenv('DB_PASSWORD'),        # Database password from environment variable
        'HOST': os.getenv('DB_HOST'),                # Database host from environment variable
        'PORT': os.getenv('DB_PORT'),                # Database port from environment variable
        'TEST': {
            'NAME': os.getenv('DB_TEST_NAME', 'test_db'),  # Optional: separate test database name
        },
    }
}


# Password validation
# https://docs.djangoproject.com/en/4.2/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',  # Prevents passwords similar to user info
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',  # Ensures minimum password length
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',  # Blocks commonly used weak passwords
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',  # Prevents all-numeric passwords
    },
]


# Internationalization
# https://docs.djangoproject.com/en/4.2/topics/i18n/

LANGUAGE_CODE = 'en-us'  # Default language for the project

TIME_ZONE = 'UTC'        # Default timezone for the application

USE_I18N = True          # Enables Django's translation system

USE_TZ = True            # Enables timezone-aware datetime handling


# Static files configuration (CSS, JavaScript, images)
# https://docs.djangoproject.com/en/4.2/howto/static-files/

STATIC_URL = 'static/'  # URL prefix for serving static files (e.g., /static/style.css)


# Default primary key field type for models
# https://docs.djangoproject.com/en/4.2/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'  # Uses 64-bit integer primary keys by default

# MEDIA FILES (for RunOutput file uploads)

MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'

# DJANGO REST FRAMEWORK

REST_FRAMEWORK = {
    # No authentication — local deployment only
    'DEFAULT_AUTHENTICATION_CLASSES': [],
    'DEFAULT_PERMISSION_CLASSES': [
        'rest_framework.permissions.AllowAny',
    ],

    # Pagination
    'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
    'PAGE_SIZE': 50,

    # Renderers — JSON + browsable API (useful for local dev)
    'DEFAULT_RENDERER_CLASSES': [
        'rest_framework.renderers.JSONRenderer',
        'rest_framework.renderers.BrowsableAPIRenderer',
    ],

    # Filtering
    'DEFAULT_FILTER_BACKENDS': [
        'rest_framework.filters.SearchFilter',
        'rest_framework.filters.OrderingFilter',
    ],
}

# 

CORS_ALLOWED_ORIGINS = ["http://localhost:5173"]  # Vite's default port

